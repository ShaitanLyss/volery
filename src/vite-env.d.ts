/* Vite's own ambient types, for the one import that is not TypeScript's to
   resolve: `?url` on an asset, which the bundler answers with the path it
   emitted. `nerd.ts` imports the symbols font that way. */
/// <reference types="vite/client" />
